# Scripts

Most flight-simulation scripts now live in `scripts/starship-mars-landing/` (SOCP guidance, bang-bang controller, 3D animation, docs). Keep new Starship/Mars landing work in that folder so outputs and archives stay contained.
